# Assets

## `staffing_config.yaml`

This file is a local copy of the staffing configuration used by this pricing skill so the skill remains self-contained.

The master source is:

`skills/ibm-bid-staffing-planner/assets/staffing_config.yaml`

When the staffing planner master config changes, refresh this local copy to keep pricing assumptions aligned.
